{
  "name": "receiver",
  "version": "1.0.0",
  "description": "Receives data from IoT Hub (event hub compatible endpoint)",
  "main": "index.js",
  "author": "Christopher Wilke",
  "license": "ISC",
  "engines": {
    "node": "<=12.0.0"
  },
  "dependencies": {
    "@azure/event-hubs": "latest",
    "dotenv": "latest"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "typescript": "~4.4.0"
  },
  "scripts": {
    "build": "tsc --build",
    "run": "node dist/index.js",
    "start": "tsc --build && node dist/index.js"
  }
}
